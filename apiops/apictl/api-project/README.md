# API project placeholder

Do not handcraft version-dependent `apictl` project metadata here. Export or generate a project with the `apictl` version matching the target WSO2 API Manager, then supply its path through `APICTL_API_PROJECT`. Backend endpoint values are supplied through version-compatible deployment parameters rather than divergent OpenAPI copies.
