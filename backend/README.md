# Backend (Future Phase)

This directory is reserved for the Python 3.12 FastAPI, SQLAlchemy, and Alembic implementation. No backend code is intentionally included in the foundation phase.

Implementation must follow the OpenAPI contract and security documents, including server-derived patient identity, deny-by-default role and object authorization, transactional duplicate-booking prevention, RFC 9457 errors, and structured audit events.
